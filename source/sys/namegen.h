












#ifndef NAMEGEN_H
#define NAMEGEN_H

#define GENNAMELEN		21

//Declare the function up here so that we can use it
// note that it does not return a value, rather it
// edits the character passed to it by reference.
void GenName(char *PlayerName);

#endif
